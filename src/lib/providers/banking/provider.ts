import type { PortfolioProviderAdapter } from '@/lib/providers/types';
export const bankingProviderPlaceholder: PortfolioProviderAdapter = { id: 'banking', name: 'Banking placeholder', async configure(){ return { ok: true, message: 'Placeholder only; no cloud sync in MVP.' }; }, async fetchAccounts(){ return []; }, async fetchHoldings(){ return []; }, async fetchBalances(){ return []; }, async sync(){ return { status: 'IDLE', message: 'Bank integration not implemented in MVP.', updatedAt: new Date().toISOString() }; }, async getSyncStatus(){ return { status: 'IDLE', updatedAt: new Date().toISOString() }; } };
