import type { PortfolioProviderAdapter } from '@/lib/providers/types';
export const ibkrProviderPlaceholder: PortfolioProviderAdapter = { id: 'ibkr', name: 'Interactive Brokers placeholder', async configure(){ return { ok: true, message: 'Placeholder only: no broker order placement or automated trading.' }; }, async fetchAccounts(){ return []; }, async fetchHoldings(){ return []; }, async fetchBalances(){ return []; }, async sync(){ return { status: 'IDLE', message: 'IBKR integration not implemented in MVP.', updatedAt: new Date().toISOString() }; }, async getSyncStatus(){ return { status: 'IDLE', updatedAt: new Date().toISOString() }; } };
